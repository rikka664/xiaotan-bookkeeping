# 小谭记账 — 产品文档与协作规则

> 本文件是「小谭记账」项目的核心文档，Claude Code 每次会话都会自动阅读。
> 它同时规定了产品设计和协作方式，是项目内所有决策的依据。

## 一、项目简介

| 项目 | 内容 |
|---|---|
| 应用名称 | 小谭记账 |
| 应用定位 | 个人桌面记账软件：记录每一笔花销，按两级分类管理，清晰看到钱花在了哪里 |
| 目标平台 | Windows 10/11、macOS（首发 Windows 安装包；Mac 安装包用户决定暂缓，代码已兼容） |
| 界面语言 | 简体中文 |
| 货币单位 | 人民币（元），金额支持两位小数 |
| 目标用户 | 记账小白也能轻松使用的普通个人用户 |
| 产品形态 | 桌面安装软件，双击安装即可使用（最终交付 .exe / .dmg 安装包） |

## 二、核心功能

### v1.0（第一个可用版本，先做这些）
1. 记一笔
   - 输入金额（元，最多两位小数）
   - 选择分类：先选一级大类，再选二级小类（两级联动）
   - 选择日期（默认今天，可修改）
   - 备注（可选填）
2. 账单列表
   - 按日期从新到旧排列
   - 按月份、按分类筛选
   - 修改 / 删除已有账单（删除前弹窗确认）
3. 统计视图
   - 本月 / 今年 / 全部 总支出
   - 各一级大类支出占比（饼图）
   - 每个分类的支出明细汇总
4. 数据安全
   - 账本保存在用户自己电脑的本地文件里，不联网、不上传
   - 支持一键导出 Excel / CSV，方便备份

### 后续版本（v1.1+，先不做，只记录想法）
- 收入记录（当前只记支出）
- 自定义分类（可自己增删分类）
- 月度预算与超支提醒
- 关键词搜索账单
- 深色模式

## 三、支出分类体系（两级）

一级大类 10 个，每个大类下有若干二级小类：

| 一级大类 | 二级小类 |
|---|---|
| 🍜 餐饮饮食 | 早餐、午餐、晚餐、夜宵、外卖、零食饮料、咖啡奶茶、水果、买菜食材、聚餐 |
| 🚌 交通出行 | 公交地铁、出租车、网约车、共享单车、火车高铁、飞机票、加油充电、停车费、过路费、车辆保养 |
| 🛒 购物消费 | 服饰鞋帽、美妆护肤、数码电子、家用电器、家居日用、母婴用品、宠物用品、烟酒 |
| 🏠 居住住房 | 房租、房贷、水费、电费、燃气费、物业费、宽带网络、维修装修 |
| 🎮 娱乐休闲 | 电影演出、游戏充值、视频会员、旅游度假、KTV酒吧、健身运动、书籍阅读 |
| 🏥 医疗健康 | 门诊看病、药品购买、住院治疗、体检保健、牙科眼科、保健品 |
| 📚 教育学习 | 学费、培训课程、书籍资料、考试报名、网课会员、文具用品 |
| 🎁 人情往来 | 红包礼金、请客送礼、婚丧嫁娶、慈善捐款 |
| 📱 通讯网络 | 手机话费、流量套餐、软件订阅、云存储服务 |
| 📦 其他支出 | 快递运费、金融手续费、保险保费、其他 |

## 四、技术方案

（2026-09-01 由用户从 Claude 提供的 3 个方案中选定：**Electron**）

| 层面 | 选型 | 大白话解释 |
|---|---|---|
| 桌面框架 | Electron | 让应用能作为桌面软件在 Windows / Mac 上运行的外壳（微信、QQ、钉钉电脑版同款技术） |
| 界面框架 | Vue 3 + Element Plus | 用来画界面和交互的"积木套装"，官方中文资料完善 |
| 构建工具 | Vite | 把代码打包成软件的"打包机" |
| 数据库 | SQLite（better-sqlite3） | 账本保存在用户电脑本地文件，不联网、不上传 |
| 开发语言 | TypeScript | 编写功能用的编程语言，能提前发现低级错误 |

选型理由（对用户的实际影响）：
- Electron：成熟稳定、开发省心；代价是安装包较大（约 80~150MB），日常使用无感。
- Vue 3 + Element Plus：中文资料多、组件现成，界面开发快且好看。
- SQLite：数据私密安全、断网可用、导出备份方便。

## 五、开发计划

| 阶段 | 内容 | 状态 |
|---|---|---|
| 1 | 技术选型（Claude 出方案，用户拍板） | ✅ 已完成（2026-09-01 选定 Electron） |
| 2 | 搭建开发环境（Claude 一步步指导用户操作） | ✅ 已完成（2026-09-01，本机 Node.js 已具备） |
| 3 | 开发核心功能：记账、账单列表、统计、导出备份 | ✅ 已完成（2026-09-01） |
| 4 | 测试与修复问题 | ✅ 核心功能已由用户验收（2026-09-01） |
| 5 | 打包 Windows 安装包 | ✅ 已完成（2026-09-01，v0.1.0 安装包 106MB） |
| 6 | 上传 GitHub 云端备份（公开仓库 + 安装包下载区） | ✅ 已完成（2026-09-01，https://github.com/rikka664/xiaotan-bookkeeping） |

注：用户于 2026-09-01 决定暂不制作 Mac 安装包。应用代码本身兼容 Mac，日后需要可随时补做。

## 六、开发备忘录（Claude 开发时必读）

- **启动命令**：一律用 `npm run dev`（会经过 scripts/launch.mjs 清理本机环境问题，不要直接运行 electron）。
- **本机环境坑**：这台电脑的进程环境里带有 `ELECTRON_RUN_AS_NODE=1`（来源不明），会直接导致 Electron 无法启动。scripts/launch.mjs 已自动处理；手动运行任何 electron 命令时要用 `env -u ELECTRON_RUN_AS_NODE`。
- **主进程热重载不生效**：electron-vite dev 对本机 src/main、src/preload 的改动监听不生效（中文路径兼容问题），改完必须重启 `npm run dev`；界面（src/renderer）改动可正常热更新。
- **停止开发服务后要清残留**：TaskStop 只杀外层进程，需再按端口清理（netstat 查 5173 监听 PID → taskkill /F /T），否则会占端口且多开 Electron 实例互抢缓存。
- **打包命令**：`npm run dist:win`（构建 + 生成 Windows 安装包，走国内镜像加速）。安装包输出到 dist/ 目录。Mac 安装包按用户决定暂不制作。
- **打包残留**：electron-builder 打包后可能在项目根目录留下一个 `xiaotan-bookkeeping/` 临时文件夹（应用复制品，非安装文件，已加入 .gitignore），打包完成后检查并删除即可。
- **本机安装注意事项**：NSIS 安装包静默安装（/S）在本机可能被 360 安全卫士拦截（2026-09-01 实测未生效）。备选方案（已验证可用）：手动安装 = 复制 dist/win-unpacked 内容到 `%LOCALAPPDATA%\Programs\小谭记账` + 用 WScript.Shell COM 创建桌面/开始菜单快捷方式 + 在 `HKCU\Software\Microsoft\Windows\CurrentVersion\Uninstall\xiaotan-bookkeeping` 登记卸载信息（卸载脚本在安装目录 uninstall.ps1）。
- **中文命令注意事项**：本机 PowerShell 的 `-c` 参数里不要直接写中文（经 bash 传递会乱码）；中文一律用环境变量传递，或写成无中文字面量的脚本。
- **数据目录**：账本数据库在 `%APPDATA%\小谭记账\xiaotan.db`（开发版与安装版共用；app.setName('小谭记账') 统一了名称，勿删）。
- **图标**：`node scripts/gen-icon.mjs` 生成 build/icon.png（纯代码绘制铜钱图标，带终端字符预览），打包时自动转为 .ico。
- **数据库**：使用 Electron 内置的 node:sqlite（Node 22 自带，无原生编译依赖），控制台会打印 ExperimentalWarning，属正常现象。
- **GitHub 云端存档**：仓库 https://github.com/rikka664/xiaotan-bookkeeping（公开）。本机已装 GitHub 官方工具 gh（位于 `C:\Program Files\GitHub CLI\gh.exe`，命令行找不到时用完整路径），已登录账号 rikka664，git 凭证已配置好（gh 自动接管）。**上传/同步必须开 VPN**（用户 2026-09-01 确认：本机长期连接国外 VPN，需要时可直接使用）：本机网络直连 GitHub 极不稳定（实测 HTTPS 频繁重置、超时），曾尝试 hosts 指定 IP 方案，已因与 VPN 冲突而全部移除。发布新版本安装包：`gh release create v0.x.x "dist/小谭记账-0.x.x-安装包.exe" --title "小谭记账 v0.x.x" --notes-file 说明文件`。**注意：GitHub 服务器会自动去掉安装包文件名里的中文**（实测标题和说明文字不受影响），所以上传前把安装包复制一份英文名（如 Xiaotan-Bookkeeping-0.x.x-Setup.exe）再作为附件上传，或在说明文字里注明实际下载文件名。本机另有未使用的 SSH 密钥 ~/.ssh/id_ed25519（SSH 走 443 端口通道已验证连通，日后 HTTPS 再出问题可用）。

## 七、协作规则（最重要，整个项目期间必须严格遵守）

1. **用户是不懂编程的完全小白，首次使用 Claude Code**，无法也不应被要求提供任何技术性要求。
2. **凡是技术问题，必须由 Claude 主动提出方案，用户决定。** 具体做法：
   - 列出 2~3 个可选方案；
   - 用大白话解释每个方案是什么、有什么优缺点；
   - 重点说明每个方案对用户的实际影响（如"安装包更大""以后扩展手机版更方便"）；
   - 最后交给用户挑选，用户拍板后严格执行。
3. **用户的选择永远优先**：即使用户选的不是技术上最优的方案，也按用户的选择执行，不得反复劝说。
4. **沟通要用大白话**：避免专业术语；必须用到术语时，当场用比喻和生活例子解释清楚。
5. **操作指引要一步一步**：凡是需要用户在电脑上动手的（安装软件、运行命令、测试应用），给出"打开什么 → 点击哪里 → 输入什么 → 会看到什么"式的详细步骤。
6. **主动汇报进度**：每完成一个重要阶段或功能，用通俗语言告诉用户做完了什么、怎么查看效果。
7. **重大决定不擅作主张**：涉及方案选择、产品设计、数据变更等大事，先说明再让用户决定。
8. **自动存档**（2026-09-01 用户已同意）：项目已加入 git 本机管理。每完成一个功能或重要改动，自动 git commit 存档（说明性中文提交信息），防止代码丢失。项目已同步到 GitHub（见开发备忘录），每次 commit 后若 VPN 已开则顺手 `git push` 同步云端；未开 VPN 时提醒用户开启。
